import { defineConfig } from "vitepress";

// https://vitepress.dev/reference/site-config
export default defineConfig({
  title: "Mazik",
  description: "MAZIKで日本語入力を改善しよう",
  themeConfig: {
    // https://vitepress.dev/reference/default-theme-config
  },
});
